sudo apt install python3-dev
python3 -m venv env
source env/bin/activate
pip install -r requirements.txt

#maybe
sudo apt-get install sqlite

python setup.py
python run.py

#open host:port (e.g. 127.0.0.1:80 in browser)
